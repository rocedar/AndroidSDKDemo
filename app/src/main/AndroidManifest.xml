<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rocedar.sdk.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".BaseApplication"
        android:theme="@style/AppTheme_Blue_White_0">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TestLoginActivity" />

        <meta-data
            android:name="ROCEDAR_APPID"
            android:value= #"这里需要填写分配的APPID"# />

        <meta-data
            android:name="ROCEDAR_APPSECRET"
            android:value= #"这里需要填写分配的APPSECRET"# />


    </application>

</manifest>